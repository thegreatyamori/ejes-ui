<ui:UiPage x:Class="EjesUI.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:EjesUI.Views.Pages"
    xmlns:models="clr-namespace:EjesUI.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DashboardPage"
    d:DataContext="{d:DesignInstance local:DashboardPage, IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    d:Background="{DynamicResource ApplicationBackgroundBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid Margin="0,10,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="14*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="340*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <ui:Button Content="Word" Command="{Binding ViewModel.ClickSaveWordCommand, Mode=OneWay}" Icon="DocumentText20" Appearance="Primary" BorderThickness="1,1,1,1" SnapsToDevicePixels="True" Grid.Column="1" Margin="4,0,0,0" VerticalAlignment="Top"/>
        <ui:Button Content="PDF" Command="{Binding ViewModel.ClickSavePDFCommand, Mode=OneWay}" Icon="DocumentPdf20" Margin="100,0,0,0" Appearance="Danger" BorderThickness="1,1,1,1" SnapsToDevicePixels="True" Grid.Column="1" Width="70" VerticalAlignment="Top"/>
        <!--<Label Content="{Binding ViewModel.Exercise, Mode=OneWay}" HorizontalAlignment="Left" Margin="200,0,0,0" Grid.Column="1" VerticalAlignment="Top" Grid.Row="1"/>-->
        <ScrollViewer Grid.Column="1" Grid.Row="1">
            <ui:Card VerticalAlignment="Top" Grid.ColumnSpan="2" Grid.Column="1" Grid.RowSpan="2" HorizontalContentAlignment="Stretch">
                <Grid>
                    <ui:VirtualizingItemsControl
                        ItemsSource="{Binding ViewModel.Buttons, Mode=OneWay}"
                        VirtualizingPanel.CacheLengthUnit="Item">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:ComponentButton}">
                                <ui:Button Command="{Binding OnClick}">
                                    <Grid>
                                        <Image Width="100" Height="100" Source="{Binding Image}" />
                                        <!--<Label Content="{Binding Title}" />-->
                                    </Grid>
                                </ui:Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ui:VirtualizingItemsControl>
                </Grid>
            </ui:Card>
        </ScrollViewer>
    </Grid>
</ui:UiPage>
